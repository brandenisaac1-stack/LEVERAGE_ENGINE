// CHRONOS recovery compatibility module.
// Production curve rendering is self-contained in index.html for this recovery build.
export function shapedSeries(){ return []; }
export function curveValueAt(){ return {tenant:NaN, landlord:NaN}; }
