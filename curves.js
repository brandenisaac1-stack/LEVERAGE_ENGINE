// Reserved curve module.
// Intentionally not imported by the recovery build.
// The proven curve renderer currently lives in app.js so authentication cannot be blocked by module dependency failures.
